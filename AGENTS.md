# Project Rules & Custom Instructions

- **No Build Execution**: Do NOT run build commands (`compile_applet`, `npm run build`, etc.). The user tests and builds the application locally on their own machine.
